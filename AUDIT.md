# Audit Register

A durable log of findings (bugs, hardening, tech-debt) and how each was closed — so audits
leave a checkable trail instead of living in chat history. Inspired by SavePoint's model.

**Rules**
- Each finding gets a stable id `F###`, never reused.
- A finding closes as **verified** only with *named proof*: a passing test, a commit that fixes
  it, or an explicit manual-verification note.
- **waived** = a deliberate decision to not fix (with the reason). The human owns waivers.
- **open** = known, not yet addressed.

**Severity:** critical · high · medium · low

| ID | Severity | Finding | Status | Proof / notes |
|----|----------|---------|--------|---------------|
| F001 | medium | Grouped territory streets drawn twice on the map; the second pass overwrote the done/not-done color | verified | Fixed in `1dd6240` — `sentStreets` excludes territory-backed entries via `entryId`. Manual map check. |
| F002 | medium | Two traces of the same road silently merged into one street entry | verified | Resolved by design in `5325993` — `ensureStreetEntry` reuses by `entryId` only + `uniqueStreetName` "(2)/(3)". Proof: `src/db.test.ts` |
| F003 | low | Streets-list territory badge can false-positive on a name collision (standalone street sharing a name with a territory street) | waived | Rare; `entryId` makes all new data collision-proof. Legacy name-fallback kept intentionally. Revisit only if it bites. |
| F004 | low | `scripture.ts` header comment claimed a space-form ("1 cor 13 4-7") normalizes, but a `chapter:verse` colon is required | verified | Fixed comment + locked behavior in `0b63717`. Proof: `src/scripture.test.ts` |
| F005 | medium | Share import (the one untrusted-input boundary) had no size caps or shape validation | verified | Hardened in `280cf5d` — encoded/file size caps, per-kind shape checks, list caps. Proof: `src/share.test.ts` |
| F006 | medium | TypeScript `strict` mode was off (null-safety not enforced) | verified | Enabled in `2f4814e`; compiles with 0 errors. Proof: `npm run build` green. |
| F007 | low | CI gated on test + typecheck but not lint | verified | `npm run lint` added to `deploy-pages.yml` in `2f4814e`. Proof: green CI run. |
| F008 | low | `Schedule.tsx` (~3k lines) and `Contacts.tsx` (~1.1k lines) are oversized; maintainability debt (not a bug) | open | Split into folders of focused files; extract pure logic into tested modules. |
| F009 | low | No Content-Security-Policy on the HTML shell | open | Optional defense-in-depth; limited by heavy inline `style={{}}` needing `'unsafe-inline'`. |
| F010 | low | No automated dependency-update mechanism | open | Consider enabling Dependabot. `npm audit` currently clean (0 vulns). |
| F011 | critical | Minute-bank quick-log wrote `timeLogs` only *after* ~1.1s of animation; a backgrounded/killed PWA in that window silently lost the logged time (and any rolled-over hour) while the bank had already decremented in localStorage | verified | Fixed in `d5c6624` — `bankQuickLogMinutes` now persists all DB rows then the bank *before* animating; animations are purely cosmetic. Gates green (`npm run build`/`lint`/`test`, 55/55). |
| F012 | high | Schedule-plan writes (`saveDayBlocks`, `removeDaySchedule`, `submitScheduledTime`/`Block`, `deleteScheduledBlock`, `clearDay`, `clearWeekSchedule`) spread a render-time `useLiveQuery` `prefs` snapshot; a second action before re-propagation could double-log time or resurrect/drop a scheduled block | verified | Fixed in `d5c6624` — all such writes route through `mutateSchedulePrefs`/a Dexie `rw` transaction that re-reads the prefs row and recomputes from the fresh copy. Gates green (`npm run build`/`lint`/`test`, 55/55). |
| F013 | high | Share import capped the *encoded* payload (256 KB) but called `inflate()` with no output limit; deflate ratios exceed 1000:1, so a crafted share could inflate to hundreds of MB and exhaust memory before `JSON.parse` or any `MAX_LIST` check ran (`REVIEW.md` F-B1) | verified | Fixed in `3217bce` — streaming `inflateCapped` aborts out of pako's `onData` past 4 MB, so the bomb is never materialized. Proof: `src/share.test.ts` builds a real deflate bomb, asserts it passes the encoded cap, and asserts rejection. |
| F014 | high | `Omit<Person,'id'\|…>` is erased at runtime, so `importSharedPayload` spread a hand-built payload's `id`/`personId`/`sharedWith` straight into `db.<table>.add()` — colliding with a real row (ConstraintError mid-import) or jumping the auto-increment counter (`REVIEW.md` F-B2) | verified | Fixed in `3217bce` — `stripInjectedKeys` removes every importer-assigned field before each write. Proof: `src/share.test.ts`. |
| F015 | medium | `getAuxConfig` built its result via `{ ...DEFAULT, ...JSON.parse(raw) }`; a present-but-wrong field overwrote its default, so a stored `{"months":null}` threw a `TypeError` out of `auxTargetHoursFor` — crashing Schedule and Reports, both of which read it every render. Reachable via restore, which writes any `fieldservice_*` string verbatim (`REVIEW.md` F-B4) | verified | Fixed in `2f984ae` — `normalizeAuxConfig` coerces each field independently. Proof: `src/auxPioneering.test.ts` asserts the exact `{"months":null}` case doesn't throw. |
| F016 | low | On the download path, "Last backup" is stamped at `a.click()` — the browser exposes no event for "the user actually saved the file" and the save dialog can be cancelled silently, so the label can overstate by one export | waived | Accepted: `a.click()` is the strongest signal that path offers, and the pre-existing "Backup file downloaded." message already carries the same imprecision. The share path is stamped only after `navigator.share` resolves, and never on `AbortError`. Revisit if the File System Access API path (PLAN 1.3) lands, which *does* confirm a write. |
| F017 | low | `.chip` computes to ~32px tall, below the 44px minimum touch target | open | Can't be grown in isolation: `.cat-pills` wraps rows with a 6px gap, so +12px either reflows every tab or makes adjacent rows' hit areas overlap and steal each other's taps. Needs a container-level rethink (or a visual-regression harness first). `.icon-btn` (44×44) and `.tabbar button` (~49px) already comply — measured in `fbd22a2`. |
| F018 | low | `ModalPortal` has no focus trap, focus restore, or Esc-to-close, so keyboard/screen-reader users can tab out of an open modal into the page behind it | open | Deferred deliberately from the `fbd22a2` a11y pass: nested modals (confirm-over-form, via the shared lock counter) mean focus must restore to the layer beneath rather than the page, and the custom NumPad/CalendarPicker complicate the focusable-element query. Needs its own task with manual keyboard testing. `// TODO(a11y)` left in `src/ModalPortal.tsx`. |
| F019 | medium | `SETTINGS_BLOCKLIST` blocked the retired `fieldservice_privacy_v1` while the live key has been `fieldservice_privacy_v2` since the pre-Meleo rename, so privacy-policy acceptance travelled inside every backup and was restored verbatim — the opposite of what the comment above the list states | verified | Fixed in `bd12f94` — added the v2 key, kept v1 for older exports. User-visible effect: restoring a backup re-shows the first-launch privacy screen once, as intended. |
| F020 | low | `fieldservice_credit_hours` and `fieldservice_theme` were read/written as bare string literals at 10 sites across 5 files, half of them without a try/catch — a throwing `localStorage` (Safari "Block All Cookies", managed profiles) or a corrupt restored value could crash a render, and each site owned its own default | verified | Fixed in `310de5d` — `src/settings.ts` owns both keys with total getters; all 10 sites routed through it. Proof: `src/settings.test.ts` (20 cases incl. a throwing storage stub). |
| F021 | low | 34 icon-only controls (modal closes, stepper arrows, view toggles, per-row actions) carried only a `title`, which screen readers treat as a low-priority fallback and touch users never see — they announced as an unnamed "button" | verified | Fixed in `fbd22a2` — `aria-label` on every one, decorative emoji `aria-hidden`, `aria-current="page"` on the active tab. |
| F022 | high | Editing a contact's address while offline (or when Nominatim can't resolve it) writes `lat/lng: undefined`, destroying a previously good coordinate — the address-cleared case is handled, the geocode-failed case is not (`REVIEW.md` F-A3) | open | Verified still present at `Contacts.tsx:486-520` on 2026-08-12. Fix: only overwrite coords when a geocode succeeds; leave the existing pin otherwise. |
| F023 | high | `StreetEntries` house edits (`updateHouse` on every keystroke, `removeHouse`, status changes) rebuild `entry.houses` from a `useLiveQuery` snapshot; two edits inside one query round-trip make the second clobber the first (`REVIEW.md` F-A4) | open | Verified still present on 2026-08-12. Fix: same pattern as F012 — mutate inside a `db.transaction` that re-reads the row; debounce the note input. |
