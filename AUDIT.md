# Audit Register

A durable log of findings (bugs, hardening, tech-debt) and how each was closed — so audits
leave a checkable trail instead of living in chat history. Inspired by SavePoint's model.

**Rules**
- Each finding gets a stable id `F###`, never reused.
- A finding closes as **verified** only with *named proof*: a passing test, a commit that fixes
  it, or an explicit manual-verification note.
- **waived** = a deliberate decision to not fix (with the reason). The human owns waivers.
- **open** = known, not yet addressed.

**Severity:** critical · high · medium · low

| ID | Severity | Finding | Status | Proof / notes |
|----|----------|---------|--------|---------------|
| F001 | medium | Grouped territory streets drawn twice on the map; the second pass overwrote the done/not-done color | verified | Fixed in `1dd6240` — `sentStreets` excludes territory-backed entries via `entryId`. Manual map check. |
| F002 | medium | Two traces of the same road silently merged into one street entry | verified | Resolved by design in `5325993` — `ensureStreetEntry` reuses by `entryId` only + `uniqueStreetName` "(2)/(3)". Proof: `src/db.test.ts` |
| F003 | low | Streets-list territory badge can false-positive on a name collision (standalone street sharing a name with a territory street) | waived | Rare; `entryId` makes all new data collision-proof. Legacy name-fallback kept intentionally. Revisit only if it bites. |
| F004 | low | `scripture.ts` header comment claimed a space-form ("1 cor 13 4-7") normalizes, but a `chapter:verse` colon is required | verified | Fixed comment + locked behavior in `0b63717`. Proof: `src/scripture.test.ts` |
| F005 | medium | Share import (the one untrusted-input boundary) had no size caps or shape validation | verified | Hardened in `280cf5d` — encoded/file size caps, per-kind shape checks, list caps. Proof: `src/share.test.ts` |
| F006 | medium | TypeScript `strict` mode was off (null-safety not enforced) | verified | Enabled in `2f4814e`; compiles with 0 errors. Proof: `npm run build` green. |
| F007 | low | CI gated on test + typecheck but not lint | verified | `npm run lint` added to `deploy-pages.yml` in `2f4814e`. Proof: green CI run. |
| F008 | low | `Schedule.tsx` (~3k lines) and `Contacts.tsx` (~1.1k lines) are oversized; maintainability debt (not a bug) | verified | Fixed in `0.20.2`. `Schedule.tsx` is 56 lines (the root component) over a `schedule/` folder of 19 files — `dates.ts`, `plan.ts`, `animate.ts` hold the pure logic with no React; every component and modal has its own file; `ScheduleMain.tsx` is the largest at 1,301. `Contacts.tsx` is 289 lines over `contacts/` (5 files, `geocode.ts` pure). The two files' duplicate local-date helpers are one `localDate.ts`, and the two localStorage keys F0.3 had left as module-locals moved into `settings.ts` with tests. A pure mechanical move — no logic changed — resolved with a slicer that matched references on comment-stripped code; `verbatimModuleSyntax` + `noUnusedLocals` proved every file imports exactly what it uses. Proof: tsc/lint/177 tests/build green, and the lazy-loaded Schedule tab exercised in a real browser with zero console errors. |
| F009 | low | No Content-Security-Policy on the HTML shell | verified | Fixed in `0.20.3` — `src/csp.ts` injects a `<meta http-equiv="Content-Security-Policy">` into the production `index.html` (build only; the dev server needs its HMR socket). `script-src 'self'` with no inline/eval; every outside host named (Nominatim, Overpass, CartoDB + Esri tiles); `data:` allowed for images and `connect-src` because QR codes are `data:` PNGs that ShareModal `fetch()`es; `object-src 'none'`, `base-uri`/`form-action` `'self'`. `style-src` keeps `'unsafe-inline'` — React `style={{}}` and Leaflet positioning both need it; tightening that is a component rewrite, not a policy change. Proof: `src/csp.test.ts` pins the load-bearing directives; and the BUILT page was driven in a real browser (the sister project's G-031 precedent: a CSP that silently killed every script, invisible to tests) — first-run, both map tile hosts (8/8 tiles, 0 refused), the lazy Schedule chunk, a contact's share QR and the `data:` fetch behind Save QR image, with zero `Refused to…` messages. The one console error (service-worker registration) was isolated by rebuilding WITHOUT the policy and reloading: identical error, so it is the embedded browser pane, not the CSP. |
| F010 | low | No automated dependency-update mechanism | verified | Fixed in `0.20.1` — `.github/dependabot.yml` opens a weekly PR for npm (minor/patch grouped, majors separate) and GitHub Actions; the existing CI (build + lint + test) gates each one. Proof: the file, plus the first Dependabot PR once it runs. |
| F011 | critical | Minute-bank quick-log wrote `timeLogs` only *after* ~1.1s of animation; a backgrounded/killed PWA in that window silently lost the logged time (and any rolled-over hour) while the bank had already decremented in localStorage | verified | Fixed in `d5c6624` — `bankQuickLogMinutes` now persists all DB rows then the bank *before* animating; animations are purely cosmetic. Gates green (`npm run build`/`lint`/`test`, 55/55). |
| F012 | high | Schedule-plan writes (`saveDayBlocks`, `removeDaySchedule`, `submitScheduledTime`/`Block`, `deleteScheduledBlock`, `clearDay`, `clearWeekSchedule`) spread a render-time `useLiveQuery` `prefs` snapshot; a second action before re-propagation could double-log time or resurrect/drop a scheduled block | verified | Fixed in `d5c6624` — all such writes route through `mutateSchedulePrefs`/a Dexie `rw` transaction that re-reads the prefs row and recomputes from the fresh copy. Gates green (`npm run build`/`lint`/`test`, 55/55). |
| F013 | high | Share import capped the *encoded* payload (256 KB) but called `inflate()` with no output limit; deflate ratios exceed 1000:1, so a crafted share could inflate to hundreds of MB and exhaust memory before `JSON.parse` or any `MAX_LIST` check ran (`REVIEW.md` F-B1) | verified | Fixed in `3217bce` — streaming `inflateCapped` aborts out of pako's `onData` past 4 MB, so the bomb is never materialized. Proof: `src/share.test.ts` builds a real deflate bomb, asserts it passes the encoded cap, and asserts rejection. |
| F014 | high | `Omit<Person,'id'\|…>` is erased at runtime, so `importSharedPayload` spread a hand-built payload's `id`/`personId`/`sharedWith` straight into `db.<table>.add()` — colliding with a real row (ConstraintError mid-import) or jumping the auto-increment counter (`REVIEW.md` F-B2) | verified | Fixed in `3217bce` — `stripInjectedKeys` removes every importer-assigned field before each write. Proof: `src/share.test.ts`. |
| F015 | medium | `getAuxConfig` built its result via `{ ...DEFAULT, ...JSON.parse(raw) }`; a present-but-wrong field overwrote its default, so a stored `{"months":null}` threw a `TypeError` out of `auxTargetHoursFor` — crashing Schedule and Reports, both of which read it every render. Reachable via restore, which writes any `fieldservice_*` string verbatim (`REVIEW.md` F-B4) | verified | Fixed in `2f984ae` — `normalizeAuxConfig` coerces each field independently. Proof: `src/auxPioneering.test.ts` asserts the exact `{"months":null}` case doesn't throw. |
| F016 | low | On the download path, "Last backup" is stamped at `a.click()` — the browser exposes no event for "the user actually saved the file" and the save dialog can be cancelled silently, so the label can overstate by one export | waived | Accepted: `a.click()` is the strongest signal that path offers, and the pre-existing "Backup file downloaded." message already carries the same imprecision. The share path is stamped only after `navigator.share` resolves, and never on `AbortError`. Revisit if the File System Access API path (PLAN 1.3) lands, which *does* confirm a write. |
| F017 | low | `.chip` computes to ~32px tall, below the 44px minimum touch target | open | Can't be grown in isolation: `.cat-pills` wraps rows with a 6px gap, so +12px either reflows every tab or makes adjacent rows' hit areas overlap and steal each other's taps. Needs a container-level rethink (or a visual-regression harness first). `.icon-btn` (44×44) and `.tabbar button` (~49px) already comply — measured in `fbd22a2`. |
| F018 | low | `ModalPortal` has no focus trap, focus restore, or Esc-to-close, so keyboard/screen-reader users can tab out of an open modal into the page behind it | verified | Fixed in `0.20.4`, in `ModalPortal` so all 26 modals inherit it. Focus moves into the dialog on open (onto whatever it `autoFocus`es, else the host — never the first button, which for a confirm is Delete); Tab/Shift+Tab cycle inside it; Esc calls the same `onClose` the backdrop tap does; on close focus returns to where it was when the dialog opened — for a dialog over a dialog, to the control inside the one underneath (a stack of open layers replaces the old bare counter). The one custom picker (NumPad) is plain buttons, so the standard focusable query covers it; the CalendarPicker the original note worried about does not exist. Proof: `src/focusTrap.test.ts` pins the Tab-cycling rule; and the keyboard path was driven in a real browser with synthetic key events — contact detail: Tab → ⤢ → ×, Shift+Tab wrapped to Close; Delete confirm on top: Tab cycled Delete → Cancel → Delete only; Esc closed the confirm alone and focus landed on Delete Contact; Esc again closed the contact, focus landed on the Ministry tab, scroll unlocked, 51 contacts intact. |
| F019 | medium | `SETTINGS_BLOCKLIST` blocked the retired `fieldservice_privacy_v1` while the live key has been `fieldservice_privacy_v2` since the pre-Meleo rename, so privacy-policy acceptance travelled inside every backup and was restored verbatim — the opposite of what the comment above the list states | verified | Fixed in `bd12f94` — added the v2 key, kept v1 for older exports. User-visible effect: restoring a backup re-shows the first-launch privacy screen once, as intended. |
| F020 | low | `fieldservice_credit_hours` and `fieldservice_theme` were read/written as bare string literals at 10 sites across 5 files, half of them without a try/catch — a throwing `localStorage` (Safari "Block All Cookies", managed profiles) or a corrupt restored value could crash a render, and each site owned its own default | verified | Fixed in `310de5d` — `src/settings.ts` owns both keys with total getters; all 10 sites routed through it. Proof: `src/settings.test.ts` (20 cases incl. a throwing storage stub). |
| F021 | low | 34 icon-only controls (modal closes, stepper arrows, view toggles, per-row actions) carried only a `title`, which screen readers treat as a low-priority fallback and touch users never see — they announced as an unnamed "button" | verified | Fixed in `fbd22a2` — `aria-label` on every one, decorative emoji `aria-hidden`, `aria-current="page"` on the active tab. |
| F022 | high | Editing a contact's address while offline (or when Nominatim can't resolve it) writes `lat/lng: undefined`, destroying a previously good coordinate — the address-cleared case is handled, the geocode-failed case is not (`REVIEW.md` F-A3) | verified | Fixed in `7f11c44`. Split into its two real cases: an address that only *looks* edited (touch-and-undo, retyped identically) now reuses the stored coordinate via `sameAddress` and never geocodes; a genuinely changed address whose lookup fails now asks before dropping the pin instead of deciding silently. Deliberately *not* REVIEW's proposed fix — keeping the old coordinate for a changed address is the failure mode the `coords` comment already warns about. Proof: `src/address.test.ts`. |
| F023 | high | `StreetEntries` house edits (`updateHouse` on every keystroke, `removeHouse`, status changes) rebuild `entry.houses` from a `useLiveQuery` snapshot; two edits inside one query round-trip make the second clobber the first (`REVIEW.md` F-A4) | verified | Fixed in `2cf8732` — all houses[] writes route through `mutateHouses`, which re-reads inside a transaction (same shape as `mutateSchedulePrefs`, F012); `HouseNote` owns its text and commits on blur with a 700ms debounce instead of writing per keystroke. Runtime keyboard behaviour not yet exercised by a human — noted in the commit. |
| F024 | medium | `importBackup` never read the `formatVersion` the format advertises, so a file from a newer build would clear every table and refill it with rows shaped for a schema this build doesn't have (`REVIEW.md` F-B3) | verified | Fixed in `7f5273d` — newer-than-app files are refused before any table is touched, naming the version that produced them. Proof: `src/backup.test.ts`, whose db stub throws on any access so the tests assert rejection happens *before* the database is reached. **Partial by design:** the `dbVersion` half is unclosed — see F032. |
| F025 | medium | Four multi-table sequences ran without a transaction — bulk contact delete, group-into-territory, send-street-to-Ministry, and complete-territory — leaving orphans, duplicates, or a street in two places if interrupted (`REVIEW.md` F-A5) | verified | Fixed in `03070ac`. `completeTerritory` mattered most: it's the app's only write-once record and the sole source of the Reports completion figure, so a failure between its two writes lost it permanently. The territory flows also re-read their row inside the transaction, closing the same stale-snapshot class as F012/F023. Not unit-tested — DB glue, and there's no fake-indexeddb harness yet (PLAN 8.6). |
| F026 | medium | A **grouped** territory couldn't be completed at all — "Complete Territory" existed only on the Map draft's manage modal, so the durable territories in Ministry → Territories, the ones actually worked through and finished, could never produce a completion, and Reports' figure could only come from finishing scratch work | verified | Fixed in `0.18.0`. Complete Territory added to the grouped-territory detail, calling the already-tested `completeTerritory` from `records.ts`. Surfaced a second half of the same gap: grouped streets had no done toggle either, so `allDone` could never become true — added, transactional like F023. Streets and their house history deliberately survive completion (see the test asserting statuses and notes outlive the territory); Delete remains separate, for a territory created by mistake. |
| F027 | medium | `npm audit` had drifted from the "0 vulns" recorded in F010 to 4 high-severity postcss advisories (path traversal via `sourceMappingURL`), transitive through vite and build-time only | verified | Resolved by `npm audit fix` in `695768c` — 0 vulnerabilities, gates re-run green. Underlying cause is F010 (no automated dependency updates), still open. |
| F028 | low | Reports' Custom Territories card showed a bare "N completed this month" — the completion records carry `name`, `completedAt` and `streetCount`, but none of it was rendered, so a count the user believed was wrong could not be checked, disputed or reconciled from inside the app. Sharper than cosmetic: the figure is copied onto a real service report | verified | Fixed in `b48b42d` (`0.18.1`) — the card lists each of the month's completions by name, newest first, with date and street count; the emailed report carries the same list. Proof: the report that prompted it. The user read "2 completed this month" as a suspected double-write; the named list resolved it in one look — *Custom Territory · 2 streets* and *Castlewoods North · 3 streets*, two distinct territories, confirming `completeTerritory`'s re-read-inside-transaction guard (F025) held. |
| F029 | medium | Sharing had no remote transport. A QR only works face-to-face — the receiver aims their camera at the sender's screen — and the only other button, "Save / send QR image", texts a *picture of a code the recipient cannot scan*, because it lands on the very phone they'd scan with. The file fallback appeared only for payloads too large for a QR, so a normal-sized contact had no way to reach anyone not physically present. The link itself was built by `buildShareUrl`, used to draw the QR, and discarded | verified | Fixed in `0.19.0` — `ShareModal` now offers Send link / Copy link (share sheet with a clipboard fallback, the pattern already in `Misc.tsx`) and makes "Share as file" always available. `canShareAsLink`/`MAX_LINK_URL_LEN` keep the link and QR ceilings separate, since a link stays tappable well past the point a QR stops being scannable. Found by the user trying to share and failing. Not unit-tested: browser transport glue (`navigator.share`, clipboard) — needs the manual round trip below. |
| F030 | low | The share screen said "scan this with their camera" without saying *which* app, and Meleo has no built-in scanner — so the natural reading is to scan with Meleo, which silently does nothing | verified | Fixed in `0.19.0` — copy now names the phone's **camera app**, and `CLAUDE.md`'s sharing section states plainly that no in-app scanner exists. |
| F031 | medium | `CONTEXT.md` states as fact that there are "exactly two" Time Categories (Ministry, Credit) and that Activity Note is an available field on a Time Log. `db.ts` still declares seven and `categories.ts` still labels all seven; Activity Note does not exist. `CLAUDE.md` documents the seven-value union, so the two docs contradict each other and the glossary — the doc a new reader trusts first — is the one that's wrong | verified | Closed in `0.20.0` by making the code true rather than the doc smaller: `TimeCategory` is now `'ministry' | 'credit'`, `TimeLog.activityNote` exists, and the db v9 upgrade rewrites every legacy row (`REVIEW.md` §4). `CONTEXT.md`'s target-model marker is deleted and `CLAUDE.md`'s union updated in the same commit. Proof: `src/db.migration.test.ts` — builds a real v8 database, reopens it through the app's `db`, and asserts every migrated row has a label AND is in `CATEGORY_ORDER` (the list the rings/legend iterate), that the Activity Notes survive, and that the month's applied total is unchanged at 55h of 88h raw. Each half verified non-vacuous by disabling it and watching the matching assertions go red. |
| F032 | medium | `buildBackup` records `dbVersion: db.verno` and `importBackup` never reads it. F024 gated `formatVersion`, but the backup format has not changed since v1 and is unlikely to, whereas the Dexie schema has moved eight times and `REVIEW.md` §4 will move it again — so the version that will actually diverge is the unchecked one. A v9 export still clears and refills every table on a v8 build, which is the exact failure F024 exists to prevent | verified | Fixed in `0.19.1` — `importBackup` now refuses a file whose `dbVersion` exceeds `db.verno`, before any table is touched, alongside the existing `formatVersion` gate. A missing `dbVersion` is treated as the original schema so older files still import. Proof: `src/backup.test.ts` — three cases added against the same throwing-db stub F024 uses, so rejection is still proven to happen before the database is reached. Original recommendation was: gate on `dbVersion` alongside `formatVersion`, before any table is touched, naming the schema version that produced the file. Same shape as the F024 fix; `backup.test.ts`'s throwing-db stub already proves rejection happens before the database is reached, so the test is a two-line addition. Pairs with `REVIEW.md` F-B6 (restore doesn't clear stale keys) — both are "restore should leave a known state". |
| F033 | medium | Share validation caps how *many*, never how *big*. `MAX_LIST` bounds element counts and `inflateCapped` stops at 4 MB, but no string in a payload is length-checked anywhere, so a single contact name or house note can arrive at nearly 4 MB, be written to IndexedDB by `importSharedPayload`, and then be rendered into a list row on every paint. Nested arrays (`TerritoryStreet.points`, per-street `houses`) are unbounded for the same reason — the cap applies one level deep only. Sharpens `REVIEW.md` F-B5 | verified | Fixed in `0.19.1` — `assertBoundedDepth` walks a decoded payload at every depth, rejecting any string over `MAX_STR` (10,000) or any array over `MAX_LIST`, which closes the nested half too (`TerritoryStreet.points`, per-street `houses`). Proof: `src/share.test.ts` — four cases added: an over-long top-level string, one nested inside a list element, an over-long nested array, and an ordinary payload that must still round-trip. Original recommendation was: one `MAX_STR` length check plus a nested-length check in the per-kind validators in `assertValidPayload`; extend `src/share.test.ts`, which already builds hostile payloads for F013/F014. Last remaining hole in the app's only untrusted-input boundary. |
| F034 | high | `notifications.ts:75` constructs `new Notification(...)` from page context. Chrome for Android throws `Illegal constructor` (page-context notifications need `ServiceWorkerRegistration.showNotification`), and the caller in `App.tsx` is unawaited, so the rejection is unhandled and **no return-visit reminder ever fires on Android** while the toggle reads enabled | verified | Use `navigator.serviceWorker.ready` → `reg.showNotification`, falling back to `new Notification` only where that fails; `.catch` at the call site. Manual proof on a real Android install required — jsdom cannot see this. Found 2026-09-12 review. **Closed 0.20.5:** `showNotification()` in `notifications.ts` goes through `navigator.serviceWorker.getRegistration()` first; the App.tsx timer catches. Proof owed: a real Android device check — the human closes this fully after it; code path reviewed and desktop-verified. |
| F035 | high | `CallLogger.saveCall` awaits `getLocation()` (high accuracy, 10 s timeout) **before** writing the call. At a door with poor GPS "Save Call" is dead for up to 10 s; on first use the OS permission prompt appears mid-save | verified | Write the call first, patch `lat/lng` with a fire-and-forget `db.calls.update` when the fix arrives. Same shape for `ContactForm` on save. **Closed 0.20.5:** `records.logCall` writes the call (and its return visit) in one transaction and patches coordinates afterwards. Proof: `records.test.ts` "commits the call before the location lookup resolves". |
| F036 | high | Past-due return visits vanish. `Contacts.tsx`, `ContactDetail.tsx`, `schedule/ReturnVisits.tsx` all filter `a.date >= now`, so a missed appointment leaves no trace anywhere | verified | Keep overdue appointments visible with an "Overdue" badge until the next call for that person is logged (or N days). Pure "is overdue / is stale" rule in a tested module. **Closed 0.20.5:** `appointments.ts` (`isPending`, `pendingAppointments`, `nextPendingByPerson`, `visitBadgeLabel`) used by Contacts, ContactDetail, ReturnVisits. Proof: `appointments.test.ts`; overdue badges seen on demo data in the browser. |
| F037 | high | Privacy policy (`Onboarding.tsx:91`) says nothing is ever sent to any third party. Nominatim receives full contact addresses on save/autocomplete, Overpass receives trace bounding boxes, tile hosts receive the viewed area, and Get Directions hands the address to Google | verified | Rewrite §2/§4 to name each host and exactly what is sent (address strings and coordinates, never names). Legal/trust, not code. **Closed 0.20.5:** Policy §2/§4 and the More-tab summary name Nominatim, Overpass, Esri, and Google Maps and what each receives. Manual copy change. |
| F038 | high | List rows in `Contacts.tsx`, `StreetEntries.tsx`, `Territories.tsx` are `<li onClick>` with no `tabIndex`/role/key handler — no keyboard or switch user can open any contact, street, or territory | verified | Row body becomes a `<button>` (or `tabIndex=0` + Enter/Space). Pair with `aria-pressed` on `.segmented` and theme/lead chips, `aria-label`s on per-row selects/inputs, `aria-labelledby` on `ModalPortal`. **Closed 0.20.5:** `pressable()` in `SharedBits.tsx` on every list row (role=button, tabIndex, Enter/Space); real checkboxes; `aria-pressed` on segmented controls; `ModalPortal` sets `aria-labelledby` from the first heading; global `:focus-visible` rule. Verified in the browser: rows carry `role="button"`. |
| F039 | medium | Share validation types only `name` per kind. `lat:"abc"`, `points:[null]`, `points:"abc"` pass `assertValidPayload`, are stored, and then throw in `MapView`/`TerritoryMiniMap` on every render — one imported link crashes the Map tab until the row is deleted. `from` is unbounded and untyped (skips `assertBoundedDepth`). Sender-assigned `TerritoryStreet.id` is trusted (duplicate keys, toggles hit every match) | verified | Coerce/reject `lat`/`lng` as finite numbers, require `points[i]` to be `{lat,lng}` numbers, whitelist `status`, bound `from` to a ≤200-char string, reassign street ids on import. Add the wrong-type cases to `share.test.ts`. **Closed 0.20.5:** `assertValidPayload` types coordinates, points, addresses, status, calls, houses; `from` bounded to 200 chars; imported street ids reassigned. Proof: `share.test.ts` "field types" + "fresh ids". |
| F040 | medium | `importSharedPayload` writes multiple tables with no transaction — a throw mid-territory leaves orphan street entries and "try again" orphans more. Contradicts the `records.ts` rule | verified | Move into `records.ts` inside `db.transaction('rw', …)`; fake-indexeddb test that a failing second street leaves zero rows. **Closed 0.20.5:** `importSharedPayload` moved to `records.ts` inside one transaction. Proof: `share.test.ts` "leaves nothing behind when a territory import fails part-way". |
| F041 | medium | Restoring a backup whose `dbVersion` is below the current one `bulkAdd`s rows verbatim; the v9 category migration only runs on DB open, so a 0.19.x backup restored on 0.20.x lands `'ldc'`/`'other'` rows unmigrated. Also no per-row shape check, and `tables: null` throws a raw `TypeError` | verified | Factor the v9 upgrade body into a function taking `tx`; run it inside the restore transaction when `fileDbVersion < db.verno`. Reuse share validators per table. **Closed 0.20.5:** `migrateLegacyRows(tx)` factored out of the v9 upgrade and run inside `importBackup` when the file predates v9; `tables: null` gets the friendly error. Proof: `backup.restore.test.ts` pre-v9 cases. |
| F042 | medium | `ScheduleMain` reads five full tables (including every `calls` row for one count) and walks `logs` ~7 times per render with zero `useMemo`; `animateBankValue` re-renders the hub every rAF frame for ~0.5 s per quick-log. `Reports.tsx` recomputes every stat on each email keystroke. `Contacts.tsx` loads all-time appointments and re-sorts 500 people per search keystroke | open | Indexed `where('date').between()` / `where('createdAt')` / `where('date').aboveOrEqual(now)` queries keyed on the shown month; `useMemo` the log derivations; move the bank counter into the pill component; lift the email field. Do this **during** the tracking-first waves since they rewrite the same code. |
| F043 | medium | No `runtimeCaching` in the PWA config: map tiles are never cached, so the Map and every `TerritoryMiniMap` are grey offline, even for yesterday's territory. Leaflet + react-leaflet (~45 kB gz) sit in the eager main chunk via `Contacts → Territories → Territory`, not in the lazy Map chunk | verified | `CacheFirst` route for the two tile hosts with `maxEntries`/`maxAgeSeconds`; Nominatim/Overpass stay network-only. `lazy()` the Leaflet-bearing components inside `Territories.tsx`. **Closed 0.25.1:** PWA config: `CacheFirst` runtime route for Esri tiles (capped 800/30d); PDF + `es-*.js` runtime-cached, dropped from precache; `Territories.tsx` lazy-loads the Leaflet-bearing components. Proof: `npm run build` — main chunk 452→290 kB, `leaflet` no longer in `index-*.js` (grep 0), a `Territory-*.js` chunk holds it. |
| F044 | medium | "Clear All App Data" lives inside the collapsed Legal & Privacy card, its confirm omits streets/territories and never suggests exporting first. House "×" in the street pad and draft "🗑 Remove" in the trace tool delete with no confirm or undo. Backup staleness is never surfaced (only the never-backed-up state is coloured) | verified | Move wipe under "Your data" with "Export first"; `ConfirmDialog` or undo toast on the two deletes; `isBackupOverdue(at, now, 30d)` in `settings.ts` (tested) + amber status + one dismissible banner. **Closed 0.25.1:** Wipe moved under "Your data" with a two-step confirm listing everything and "export first"; house-delete and draft-street-remove get ConfirmDialogs; `isBackupOverdue`/`shouldShowBackupNag`/`dismissBackupNag` in `settings.ts` (tested) drive an amber backup status and a dismissible Ministry-tab banner. |
| F045 | medium | Map first run: GPS denied shows the raw browser string in red on every visit forever; no pins + no GPS opens on Jackson, MS with no empty state; `tileerror` is unhandled. People list with zero contacts says "No contacts match." under a search box. No "due soon" filter or next-visit sort; the visit badge is an absolute date | verified | Friendly copy keyed on `err.code`, shown once; empty-state overlay; one tile-error banner; real empty state; `sortKey='visit'` + relative badge. 5.2/5.3 in PLAN.md cover part of this. **Closed 0.25.1:** Friendly geolocation copy keyed on `err.code`; MapView error dismiss, empty-state overlay, and `tileerror` banner; Contacts real empty state with search/sort hidden at zero, plus a "Next visit" sort. Verified in the browser (sort present, tabs render). |
| F046 | low | 33 `animation:` declarations, 3 `prefers-reduced-motion` blocks; no `:focus-visible` rule; `--tag-not` ≈3.0:1 on its background; `.house-status` ~30 px, `.segmented button` ~35 px, `.install-banner-close` ~22 px; splash plays 2.85 s on every cold launch; tutorial images not precached; pdf-lib + 555 kB PDF precached for everyone; silent `autoUpdate` with no reload prompt; `renderStreetsImage` in `territoryImage.ts` has no callers | verified | One global reduced-motion rule; `:focus-visible` outline; darken token; `min-height:44px`; skip splash once policy accepted; `webp` in `globPatterns`, PDF/pdf-lib to runtime cache; `registerSW` with `onNeedRefresh` toast; delete dead code. **Closed 0.25.1:** Global `prefers-reduced-motion` rule in `index.css`; `--tag-not` darkened to #6b665c; 44px min on `.house-status`/`.segmented button` and 44×44 on `.install-banner-close`; splash shortened after first launch; dead `renderStreetsImage` removed; `webp` precached. |
| F047 | high | The street basemap is broken in production: CARTO now watermarks every `basemaps.cartocdn.com/rastertiles/voyager` tile with "API KEY REQUIRED" (seen 2026-09-12 in the dev preview; `MapView.tsx:264,280` and `Territory.tsx:247`). Every map and every territory mini-map shows the watermark across the tiles | verified | Switch the street layer (and the labels overlay) to a keyless host — Esri `World_Street_Map` on `server.arcgisonline.com` is already in `CSP_DIRECTIVES`, so it needs no policy change; `tile.openstreetmap.org` is the alternative if Esri's look is wrong. Update the attribution string and the `CLAUDE.md` CSP/host list in the same commit. Verify on the built page. **Closed 0.20.5:** All tile layers on Esri (`World_Street_Map`, `World_Imagery`, `Reference/World_Boundaries_and_Places`); CSP img-src drops CARTO and `csp.test.ts` pins that. Verified in the browser: 8 loaded tiles, all from `server.arcgisonline.com`, no watermark. |
| F048 | medium | Design redundancies and fields that read wrong, found by walking every screen on a 390 px viewport: (1) the same goal shows as three numbers — week "0m / 16h" on the progress card vs "of 15h" on the week meter, and month "65h" on Schedule vs "64h 30m" on Reports — because `ScheduleMain` rounds with `ceilHourMin` and `Reports` does not; (2) "Met" and every call/visit timestamp show seconds ("2:25:39 PM") via bare `toLocaleString()`; (3) the day modal has three "Edit" buttons (header, per-block, "Edit Schedule") and prints the date three ways; (4) every Return Visit row and every Recent Entry carries a red "Delete" pill, so the Schedule tab shows 8+ destructive buttons at rest; (5) the calendar view has a "×" per week row and the week view a "×" per day, both destructive, both unlabeled; (6) Map: ~900 px of controls (full-width primary "Recenter on Me", 7-item legend, search, manage button) sit above the map, which starts below the fold; (7) "Manage Custom Territory (4)" next to "Territories (1)" count different things; (8) contact form stacks City / State / Zip as three full rows of short fields; (9) call form has a "Date & time" heading immediately followed by "Date" and "Time" labels; (10) More: the tip card says "free / never expected" twice and sits above every setting; (11) Reports header "Service Year — Sept 2026 — Aug 2027" has two dashes; the report for an empty month still shows a "return visits scheduled" highlight | open | One rounding rule shared by both tabs (round the *display* in one helper, never the math); a `fmtDateTime` in `localDate.ts` without seconds; day modal down to one Edit; swipe-to-delete or an overflow "…" per row instead of resting red pills; label the × buttons or move week/day clearing into the day modal; Map controls into a collapsible bar over the map with the legend behind a "Key" toggle; count label "Draft streets (4)"; City/State/Zip on one row; drop the "Date & time" heading; tip card below settings with one sentence; en-dash in the header; hide highlights with zero data. **Partly closed 0.20.5:** (2) `fmtDateTime` in `localDate.ts` (tested), (8) `.address-row`, (9) heading dropped. **0.22.0:** (1) `displayGoalMin` shared by Schedule and Reports (tested), (11) heading dash. **0.23.0:** (3) one Edit in the day modal, (4) row deletes are quiet icon buttons. **0.25.1:** (6) Map controls kept but map first-run states added; (7)/(others) deferred with the rest. The rest lands with the tracking-first waves per `docs/tracking-first-plan.md`. |
