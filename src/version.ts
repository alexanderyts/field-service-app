// App version stamp — bump alongside package.json's "version". Surfaced in the More tab
// and written into every data backup so a future version (or the final app-store build)
// can tell which release produced a given export and migrate it if needed.
export const APP_VERSION = '0.17.0'
